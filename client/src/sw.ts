/// <reference lib="webworker" />

// This file is no longer used for service worker.
// The actual service worker is in /public/sw.js
// We keep this file to avoid build errors but it won't be loaded
console.log("This compiled service worker is not in use. See /public/sw.js instead.");

export {};
